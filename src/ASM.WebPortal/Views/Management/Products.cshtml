@model ProductsPageViewModel
@{
    ViewData["Title"] = "Products";
}
<section class="panel-card p-4 mb-4">
    <div class="dashboard-section-head">
        <div>
            <div class="dashboard-section-title">Tìm vị trí sản phẩm</div>
            <div class="dashboard-section-subtitle">Tra cứu theo SKU hoặc tên sản phẩm trong tenant hiện tại.</div>
        </div>
    </div>

    <form asp-action="Products" method="get" class="row g-3 align-items-end">
        <div class="col-lg-8">
            <label class="form-label" for="product-location-keyword">Từ khóa</label>
            <input
                id="product-location-keyword"
                name="keyword"
                value="@Model.Keyword"
                class="form-control"
                placeholder="Nhập SKU hoặc tên sản phẩm" />
        </div>
        <div class="col-lg-4 d-flex gap-2">
            <button class="btn btn-dark flex-fill">Tìm vị trí</button>
            @if (Model.HasSearched)
            {
                <a class="btn btn-outline-secondary" asp-action="Products">Xóa</a>
            }
        </div>
    </form>

    @if (Model.HasSearched)
    {
        <div class="border-top pt-4 mt-4">
            @if (Model.LocationResults.Any())
            {
                <div class="table-responsive">
                    <table class="table align-middle">
                        <thead>
                            <tr>
                                <th>SKU</th>
                                <th>Sản phẩm</th>
                                <th>Số lượng</th>
                                <th>Pallet</th>
                                <th>Trạng thái</th>
                                <th>Kho</th>
                                <th>Area</th>
                                <th>Rack</th>
                                <th>Slot</th>
                                <th>Vị trí đầy đủ</th>
                                <th>Ghi chú</th>
                            </tr>
                        </thead>
                        <tbody>
                            @foreach (var item in Model.LocationResults)
                            {
                                <tr>
                                    <td><span class="fw-semibold">@item.Sku</span></td>
                                    <td>@item.ProductName</td>
                                    <td>@(item.InventoryItemId.HasValue ? item.Quantity : "-")</td>
                                    <td>@(item.PalletCode ?? "-")</td>
                                    <td>@(item.PalletStatus ?? "-")</td>
                                    <td>@(item.WarehouseName ?? "-")</td>
                                    <td>@(item.AreaName ?? "-")</td>
                                    <td>@(item.RackName ?? "-")</td>
                                    <td>@(item.SlotName ?? "-")</td>
                                    <td>
                                        @if (string.IsNullOrWhiteSpace(item.LocationPath))
                                        {
                                            <span class="text-muted">-</span>
                                        }
                                        else
                                        {
                                            <span>@item.LocationPath</span>
                                        }
                                    </td>
                                    <td>@(item.Note ?? "-")</td>
                                </tr>
                            }
                        </tbody>
                    </table>
                </div>
            }
            else
            {
                <div class="layout-empty-state">Không tìm thấy sản phẩm.</div>
            }
        </div>
    }
</section>

<div class="row g-4">
    <div class="col-lg-4">
        <div class="panel-card p-4">
            <form asp-action="CreateProduct" method="post" class="form-grid">
                <input asp-for="Sku" class="form-control" placeholder="SKU" />
                <input asp-for="Name" class="form-control" placeholder="Name" />
                <textarea asp-for="Description" class="form-control" placeholder="Description"></textarea>
                <button class="btn btn-dark">Save Product</button>
            </form>
        </div>
    </div>
    <div class="col-lg-8">
        <div class="panel-card p-4">
            <table class="table align-middle">
                <thead><tr><th>Product</th><th>Description</th><th>QR Preview</th></tr></thead>
                <tbody>
                @foreach (var item in Model.Products)
                {
                    <tr>
                        <td>
                            <div class="fw-semibold">@item.Name</div>
                            <div class="small text-muted">@item.Sku</div>
                        </td>
                        <td>@item.Description</td>
                        <td>
                            @if (Model.QrCodes.TryGetValue(item.Id, out var qr))
                            {
                                <div class="qr-preview-card qr-preview-card-row">
                                    <img class="qr-preview-image qr-preview-image-compact" src="@Url.Action("DownloadQr", "Management", new { qrId = qr.Id })" alt="QR for product @item.Sku" loading="lazy" />
                                    <a class="btn btn-sm btn-outline-dark w-100" asp-action="DownloadQr" asp-controller="Management" asp-route-qrId="@qr.Id">Download QR</a>
                                </div>
                            }
                        </td>
                    </tr>
                }
                </tbody>
            </table>
        </div>
    </div>
</div>
